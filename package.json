{
  "private": true,
  "name": "postinder",
  "version": "2.0.0",
  "packageManager": "npm@11.13.0",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix apps/admin\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
