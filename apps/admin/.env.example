# Localmente, deixe como /api/v1 para usar o proxy do Vite.
VITE_API_URL=/api/v1
VITE_DEPLOYMENT_MODE=production

# Em producao, use a URL publica do backend.
# Exemplo:
# VITE_API_URL=https://api-postinder.exemplo.com/api/v1
